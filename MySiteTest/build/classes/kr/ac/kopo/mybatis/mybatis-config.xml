<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- DB 설정 파일 로드 -->
    <properties resource="kr/ac/kopo/mybatis/db.properties" />
    
    <!-- Type Aliases 설정 -->
    <typeAliases>
        <typeAlias type="kr.ac.kopo.board.vo.CompanyInfoVO" alias="CompanyInfoVO" />
        <typeAlias type="kr.ac.kopo.board.vo.FAQVO" alias="FAQVO" />
        <typeAlias type="kr.ac.kopo.board.vo.ProductVO" alias="ProductVO" />
        <typeAlias type="kr.ac.kopo.board.vo.UserVO" alias="UserVO" />
    </typeAliases>

    <!-- 데이터베이스 환경 설정 -->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}" />
                <property name="url" value="${jdbc.url}" />
                <property name="username" value="${jdbc.user}" />
                <property name="password" value="${jdbc.password}" />
            </dataSource>
        </environment>
    </environments>

    <!-- Mapper 파일 설정 -->
    <mappers>
    <mapper resource="common/db/UsersMapper.xml" />
    <mapper resource="common/db/FAQMapper.xml" />
    <mapper resource="common/db/ProductsMapper.xml" />
</mappers>
</configuration>
